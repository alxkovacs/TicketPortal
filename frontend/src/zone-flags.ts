// Zone.js konfigurációs beállítások
// További információ: https://angular.io/guide/zone

// Példa beállítások:
// (window as any).__Zone_disable_requestAnimationFrame = true; // requestAnimationFrame letiltása
// (window as any).__Zone_disable_on_property = true; // onProperty események letiltása (pl. onclick)
// (window as any).__zone_symbol__UNPATCHED_EVENTS = ['scroll', 'mousemove']; // meghatározott események letiltása

// IE/Edge fejlesztői eszközök esetén:
// (window as any).__Zone_enable_cross_context_check = true; 